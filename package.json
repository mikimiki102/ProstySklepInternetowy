{
  "name": "projekt-2",
  "private": true,
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c auto \"npm --prefix backend run start:dev\" \"npm --prefix frontend run dev\""
  }
}
